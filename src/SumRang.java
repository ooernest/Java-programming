public class SumRang {
    public static void main(String[] args) {
        //        Created an array Variable and name it "sum" that stores list of integers.
//        To add Value inside an array you need "{ add value here}"
        int[] sum = {2, 5, 8, 1, 0, 4, 78, 34, 21};

//        Tell the forloop to start from the beginning of the array, then
//        tell it to stop at the lenghth of the array and of course, tell it to kick start the counting
        for(int i = sum.length; i <= sum.length; i ++){
            System.out.println("This is the array lenghth" +i);
        }
    }
}
